/**
 * Re-export of the shared Airtable type map. Source lives at
 * `src/shared/airtable/type-map.ts` and is consumed by both the server
 * ingestion path and the browser-side sync in `github-matrix-dev/app/`.
 */
export * from '../shared/airtable/type-map.js';
